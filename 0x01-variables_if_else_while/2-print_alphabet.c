#include <stdio.h>

/**
 * main - Prints the alphabet in lowercase
 * Return: Always (Success)
 */

int main(void)
{
	char k;

	for (k = 'a'; k <= 'z'; k++)
	putchar(k);
	putchar('\n');
	return (0);
}
